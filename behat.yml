default:
  extensions:
    Behat\MinkExtension:
      sessions:
        default:
          goutte: ~

  suites:
    default:
      paths: [ '%paths.base%/tests/features' ]
      contexts: [ Tests\Features\FeatureContext ]
